    series_up_pct: dict[datetime, float] = {}
    series_down_pct: dict[datetime, float] = {}
    tracked = len(open_floor)
    for bucket in sorted(hourly_latest.keys()):
        up_n = 0
        down_n = 0
        for variant_id, floor in hourly_latest[bucket].items():
            base = open_floor.get(variant_id)
            if base is None or base <= 0:
                continue
            pct = (floor - base) / base * 100.0
            if pct > _ITEM_FLOOR_PCT_EPSILON:
                up_n 